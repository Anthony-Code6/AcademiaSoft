@{
    ViewData["Title"] = "Alumnos | Editar";
}

@model AcademiaSoft.Models.Alumno

<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">Alumno | Editar</h1>
            </div>
        </div>
    </div>
</div>

<div class="content">
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-6 col-sm-12">
                <div class="card card-primary card-outline">
                    <div class="card-header">
                        <h4 class="card-title"><i class="fas fa-edit"></i> Editar Alumnos</h4>
                    </div>
                    <form asp-action="Edit" asp-controller="Alumno" method="post" enctype="multipart/form-data">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="card-body">
                            <div class="form-group">
                                <label class="form-label" asp-for="Documento">Documento</label>
                                <input asp-for="Id" type="hidden"  />
                                <input asp-for="Documento" class="form-control" />
                                <span asp-validation-for="Documento" class="text-danger"></span>
                            </div>
                            <div class="form-group">
                                <label class="form-label" asp-for="Nombre">Nombre</label>
                                <input asp-for="Nombre" class="form-control" />
                                <span asp-validation-for="Nombre" class="text-danger"></span>
                            </div>
                            <div class="form-group">
                                <label class="form-label" asp-for="Apellido">Apellido</label>
                                <input asp-for="Apellido" class="form-control" />
                                <span asp-validation-for="Apellido" class="text-danger"></span>
                            </div>
                            <div class="form-group">
                                <label class="form-label" asp-for="Idusuario">Usuario</label>
                                <select class="form-control" asp-for="Idusuario" >
                                    <option value="0">-- Selecciona el Usuario --</option>
                                    @foreach (var item in ViewBag.Usuarios)
                                    {
                                        <option value="@item.Id">@item.Correo - @item.Tipo</option>
                                    }
                                </select>
                                <span asp-validation-for="Idusuario" class="text-danger"></span>


                            </div>
                        </div>
                        <div class="card-footer text-end">
                            <a href="@Url.ActionLink("Index","Alumno")" class="btn btn-default">Regresar</a>
                            <button type="submit" class="btn btn-warning">Actualizar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>



@{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}


@section scripts {
    <script src="~/plugins/bs-custom-file-input/bs-custom-file-input.min.js"></script>
}